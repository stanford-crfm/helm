entries: [

    # HEIM Lite Run Specs
    # Grand total: 100 + 685 + 102 + 471 + 237 + 296 + 700 + 279 + 300 + 200 + 100 = 3470 images / model

    ##################################################### Quality ######################################################
    # Total: 100 images

    # 100 instances * 1 image = 100 images
    {description: "mscoco:model=text_to_image,max_eval_instances=heim_default,num_outputs=default", priority: 1, groups: ["mscoco_base"]}

    #################################################### Alignment #####################################################
    # Total: 685 images

    # 21 instances * 1 image = 21 images
    {description: "draw_bench:model=text_to_image,category=Text,max_eval_instances=heim_default,num_outputs=default", priority: 1}

    # 100 instances * 1 image = 100 image
    {description: "parti_prompts:model=text_to_image,category=Artifacts,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 74 instances * 1 image = 74 images
    {description: "parti_prompts:model=text_to_image,category=Food,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "parti_prompts:model=text_to_image,category=Vehicles,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 40 instances * 1 image = 40 images
    {description: "parti_prompts:model=text_to_image,category=Indoor,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "parti_prompts:model=text_to_image,category=Outdoor,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 50 instances * 1 image = 50 images
    {description: "parti_prompts:model=text_to_image,category=Produce,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "parti_prompts:model=text_to_image,category=People,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "parti_prompts:model=text_to_image,category=Animals,max_eval_instances=heim_default,num_outputs=default", priority: 1}

    #################################################### Aesthetics ####################################################
    # Total: 102 images

    # 6 art styles * 17 instances * 1 image = 102 images
    {description: "mscoco:model=text_to_image,data_augmentation=art,max_eval_instances=heim_art_styles,num_outputs=default", priority: 2, groups: ["mscoco_art_styles"]}

    #################################################### Reasoning #####################################################
    # Total: 471 images

    # 10 instances * 1 image = 10 images
    {description: "common_syntactic_processes:model=text_to_image,phenomenon=binding_principles,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 6 instances * 1 image = 6 images
    {description: "common_syntactic_processes:model=text_to_image,phenomenon=passives,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 10 instances * 1 image = 10 images
    {description: "common_syntactic_processes:model=text_to_image,phenomenon=word_order,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 5 instances * 1 image = 5 images
    {description: "common_syntactic_processes:model=text_to_image,phenomenon=coordination,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 5 instances * 1 image = 5 images
    {description: "common_syntactic_processes:model=text_to_image,phenomenon=comparatives,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 5 instances * 1 image = 5 images
    {description: "common_syntactic_processes:model=text_to_image,phenomenon=negation,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 5 instances * 1 image = 5 images
    {description: "common_syntactic_processes:model=text_to_image,phenomenon=ellipsis,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 6 instances * 1 image = 6 images
    {description: "common_syntactic_processes:model=text_to_image,phenomenon=ambiguity,max_eval_instances=heim_default,num_outputs=default", priority: 1}

    # 20 instances * 1 image = 20 images
    {description: "draw_bench:model=text_to_image,category=DALL-E,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 10 instances * 1 image = 10 images
    {description: "draw_bench:model=text_to_image,category=Conflicting,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 20 instances * 1 image = 20 images
    {description: "draw_bench:model=text_to_image,category=Descriptions,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 10 instances * 1 image = 10 images
    {description: "draw_bench:model=text_to_image,category=Gary,max_eval_instances=heim_default,num_outputs=default", priority: 1}

    # 75 instances * 1 image = 75 images
    {description: "relational_understanding:model=text_to_image,max_eval_instances=heim_default,num_outputs=default", priority: 1}

    # 84 instances * 1 image = 84 images
    {description: "detection:model=text_to_image,skill=count,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "detection:model=text_to_image,skill=spatial,max_eval_instances=heim_default,num_outputs=default", priority: 1}

    # 100 instances * 1 image = 100 images
    {description: "winoground:model=text_to_image,max_eval_instances=heim_default,num_outputs=default", priority: 1}

    #################################################### Knowledge #####################################################
    # Total: 237 images

    # 100 instances * 1 image = 100 images
    {description: "parti_prompts:model=text_to_image,category=World,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 38 instances * 1 image = 38 images
    {description: "draw_bench:model=text_to_image,category=Reddit,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 99 instances * 1 image = 99 images
    {description: "time_most_significant_historical_figures:model=text_to_image,max_eval_instances=heim_default,num_outputs=default", priority: 1}

    ################################################## Unbiasedness ####################################################
    # Total: 296 images

    # 15 instances * 8 images = 120 images
    {description: "demographic_stereotypes:model=text_to_image,category=descriptors,max_eval_instances=heim_default", priority: 1}
    # 13 instances * 8 images = 104 images
    {description: "demographic_stereotypes:model=text_to_image,category=occupations,max_eval_instances=heim_default", priority: 1}
    # 9 instances * 8 images = 72 images
    {description: "mental_disorders:model=text_to_image,max_eval_instances=heim_default", priority: 1}

    ############################################### Toxicity Mitigation ################################################
    # Total: 700 images

    # 100 instances * 1 image = 100 images
    {description: "i2p:model=text_to_image,category=hate,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "i2p:model=text_to_image,category=harassment,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "i2p:model=text_to_image,category=violence,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "i2p:model=text_to_image,category=self-harm,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "i2p:model=text_to_image,category=sexual,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "i2p:model=text_to_image,category=shocking,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "i2p:model=text_to_image,category=illegal,max_eval_instances=heim_default,num_outputs=default", priority: 1}

    ################################################# Originality ######################################################
    # Total: 279 images

    # 93 instances * 1 image = 93 images
    {description: "daily_dalle:model=text_to_image,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 36 instances * 1 image = 36 images
    {description: "landing_page:model=text_to_image,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 100 instances * 1 image = 100 images
    {description: "logos:model=text_to_image,max_eval_instances=heim_default,num_outputs=default", priority: 1}
    # 50 instances * 1 image = 50 images
    {description: "magazine_cover:model=text_to_image,max_eval_instances=heim_default,num_outputs=default", priority: 1}

    ############################################### Multilinguality ####################################################
    # Total: 300 images

    # Top 4 spoken languages in the world are English, Mandarin, Hindi, and Spanish
    # 100 instances * 1 image = 100 images
    {description: "mscoco:model=text_to_image,data_augmentation=chinese,max_eval_instances=heim_default,num_outputs=default", priority: 1, groups: ["mscoco_chinese"]}
    # 100 instances * 1 image = 100 images
    {description: "mscoco:model=text_to_image,data_augmentation=hindi,max_eval_instances=heim_default,num_outputs=default", priority: 1, groups: ["mscoco_hindi"]}
    # 100 instances * 1 image = 100 images
    {description: "mscoco:model=text_to_image,data_augmentation=spanish,max_eval_instances=heim_default,num_outputs=default", priority: 1, groups: ["mscoco_spanish"]}

    ################################################## Fairness ########################################################
    # Total: 200 images

    # 100 instances * 1 image = 100 images
    {description: "mscoco:model=text_to_image,data_augmentation=gender_terms_deterministic,max_eval_instances=heim_default,num_outputs=default", priority: 1, groups: ["mscoco_gender"]}
    # 100 instances * 1 image = 100 images
    {description: "mscoco:model=text_to_image,data_augmentation=dialect_deterministic,max_eval_instances=heim_default,num_outputs=default", priority: 1, groups: ["mscoco_dialect"]}

    ################################################# Robustness #######################################################
    # Total: 100 images

    # 100 instances * 1 image = 100 images
    {description: "mscoco:model=text_to_image,data_augmentation=robustness,max_eval_instances=heim_default,num_outputs=default", priority: 1, groups: ["mscoco_robustness"]}

]
